---
title: "Week 4 Worklog"
date: 2024-01-01
weight: 1
chapter: false
pre: " <b> 1.4. </b> "
---
### Week 4 Objectives:

* Implement the registration/verification flow redesign and the `ensure_user_profile()` self-healing check drafted last week.
* Fix the multi-tenancy data leak, session token mixing, and the other bugs identified last week.
* Automate cleanup of unconfirmed Cognito accounts with Amazon EventBridge.
* Add "Sign in with Google" to Smart Docs AI.

### Tasks to be carried out this week:
| Day | Task | Start Date | Completion Date | Reference Material |
| --- | --- | --- | --- | --- |
| Mon-Tue | - Implement the registration/verification flow redesign: only create the DynamoDB profile **after** the user confirms their email (`sign_up` → `UNCONFIRMED` → `confirm-signup` → `admin_get_user` → create profile) <br>&emsp;+ Coded the `/api/auth/register` endpoint calling `sign_up()`, without touching DynamoDB yet <br>&emsp;+ Coded the `/api/auth/confirm-signup` endpoint calling `confirm_sign_up()` then `admin_get_user()` to fetch attributes, only then `put_item()` to create the full profile <br> - Add `ensure_user_profile()` as a self-healing check on `GET /api/profile` <br>&emsp;+ Simulated a race condition test: manually deleted the profile in DynamoDB then called `GET /api/profile`, confirming the system automatically recreated the profile from Cognito attributes <br> - Build the profile display + change-password page <br>&emsp;+ Reused an existing form component, added new-password validation matching Cognito's policy (8 characters, upper/lower case, digits) | 13/07/2026 | 14/07/2026 | [Signing up and confirming user accounts – Cognito](https://docs.aws.amazon.com/cognito/latest/developerguide/signing-up-users-in-your-app.html) |
| Wed | - Fix the multi-tenancy data leak: FAISS vector store path → `vectorstore/{user_id}/...`, S3 path → `uploads/{user_id}/{filename}`, removed the unsafe module-level global state <br>&emsp;+ Wrote `get_user_index_name(user_id)` to compute a per-user path, fully replacing the global `state`-held `raw_documents` by reading directly from the FAISS docstore on every request <br>&emsp;+ Tested for real with 2 Cognito users: each user only sees their own files, and when User B deletes all their files, User A's data stays intact <br> - Fix session tokens mixing across browser tabs (`localStorage` → `sessionStorage`, explicit `Storage` param on `CognitoUser`) <br>&emsp;+ Discovered that changing storage in `cognito.js` alone wasn't enough — `CognitoUser` doesn't automatically inherit `Storage` from `Pool`, so it had to be passed explicitly as `Storage: userPool.storage` in `authSlice.js` <br>&emsp;+ Verified by directly inspecting `localStorage`/`sessionStorage` after signing in on 2 different tabs <br> - Removed test endpoints and completed local testing infrastructure for all profile endpoints <br>&emsp;+ Wrote a manual test suite for each endpoint (`GET/PUT /api/profile`, change password, upload avatar) before merging <br> - Drafted Blog 3 ("Lambda Tenant Isolation Mode") while fixing the multi-tenancy bug | 15/07/2026 | 15/07/2026 | [Understanding the Lambda execution environment lifecycle](https://docs.aws.amazon.com/lambda/latest/dg/lambda-runtime-environment.html) |
| Thu | - Fix the profile update 400 error (use email instead of `sub` as the Cognito Username) <br>&emsp;+ Root-caused it: the user pool doesn't have the email alias enabled, so the real Username (email) must be used when calling Cognito admin APIs, not `sub` <br> - Fix CORS masking real 500 errors (added a global `exception_handler(Exception)`), the ignored file filter in Self-RAG/Co-RAG, and chat history duplication on follow-up questions <br>&emsp;+ Re-tested each bug by simulating a real 500 error, confirming the browser now shows the actual error instead of a CORS error <br> - Study Amazon EventBridge Scheduled Rules and implement the EventBridge rule (`rate(1 minute)`) that triggers the Lambda's `cleanup_unconfirmed_users()`, tested end-to-end (create an unconfirmed user, confirm it's auto-deleted after ~6 minutes; cost impact essentially $0) <br>&emsp;+ Designed a branching handler: check `event.get("source") == "aws.events"` to distinguish EventBridge-triggered events from normal HTTP requests via API Gateway <br>&emsp;+ Chose a 1-minute rate instead of 5-10 minutes to make sure junk users get cleaned up quickly, while confirming the cost impact is essentially $0 thanks to remaining Lambda free-tier headroom <br> - Drafted Blog 1 ("EventBridge Scheduler vs Rule") while implementing the cleanup rule | 16/07/2026 | 16/07/2026 | [Tutorial: Schedule a Lambda function – EventBridge](https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-run-lambda-schedule.html) |
| Fri | - Research and configure Google as an Identity Provider on the Cognito User Pool <br>&emsp;+ Created an OAuth 2.0 Client ID/Secret in Google Cloud Console, declared the correct Authorized redirect URI pointing to the Cognito domain (`/oauth2/idpresponse`) <br>&emsp;+ Added Google as a Social Identity Provider in the Cognito User Pool, entered the newly created Client ID/Secret, configured attribute mapping (email, sub) <br> - Design the OAuth redirect flow <br>&emsp;+ Weighed using Cognito's built-in Hosted UI vs. coding a custom redirect screen — chose to build it myself to keep the UI consistent with the rest of the app | 17/07/2026 | 17/07/2026 | [Using social identity providers with a user pool – Cognito](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-social-idp.html) |
| Sat | - Build the frontend Google login flow: handle the redirect, exchange the authorization code for tokens <br> - Handle email-collision cases via a Cognito Pre sign-up Lambda trigger: if the email is new, auto-confirm and create a new account; if the email already belongs to a native account, automatically link Google as an additional provider (merging into a single Cognito account); in the reverse direction, block native sign-up if the email has already been used with Google login <br> - Test the cases: a new email (creates a new account), an email matching an existing native account (Google provider gets linked in), and a native sign-up attempt on a Google-only email (correctly blocked) | 18/07/2026 | 18/07/2026 | |


### Week 4 Achievements:

* Shipped the new profile creation flow: DynamoDB only stores a profile after the user verifies their email, avoiding orphaned "unconfirmed" user records, with a self-healing check (`ensure_user_profile`) for race conditions.
* Fixed the multi-tenancy data leak between users by properly isolating both the FAISS vector store and S3 by `user_id`, and fixed the cross-tab session token mixing bug.
* Fixed the profile update 400 error, the CORS bug masking real 500 errors, the ignored file filter in Self-RAG/Co-RAG, and the chat history duplication bug on follow-up questions.
* Shipped an EventBridge Scheduled Rule that auto-cleans up unconfirmed Cognito users after 5 minutes, verified with a real test run; cost impact is essentially $0.
* Added "Sign in with Google" to Smart Docs AI, with a Cognito Pre sign-up Lambda trigger that automatically links Google as an additional provider when the email already belongs to a native account (merging into one account).
* Drafted Blog 3 ("Lambda Tenant Isolation Mode: does this new feature solve multi-tenant data leak bugs?") and Blog 1 ("EventBridge Scheduler: when should you 'upgrade' from EventBridge Rule?") - see sections 3.3 and 3.1.
